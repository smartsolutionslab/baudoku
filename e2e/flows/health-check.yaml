appId: com.baudoku.app
---
# Health Check Flow - Verifies the app launches and home screen loads

- launchApp

# CI emulator is slow (software rendering, cold boot) — app startup
# (Hermes init + font loading + SQLite migrations) can take 30-40s
- extendedWaitUntil:
    visible: "Projekte"
    timeout: 60000
